#include <stdio.h>
#include "main.h"

/**
 * print_to_98 - prints to 98
 * @n: the starting point
 * Return: no return
 */
void print_to_98(int n)
{
	int n;
	
	for (n == 98, n <= 98, n++)
	{
		if (n == 98)
			printf("%d\n", n);
		else
			printf("%d, ", n);
		{
			if (n == 98, n >= 98, n--)
				printf("%d\n", n);
			else
				printf("%d, ", n);
		}
	}
}
